name: Generate Profile Stats

on:
  schedule:
    - cron: '0 0 * * *' # Daily at midnight
  workflow_dispatch: # Allows manual trigger

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Create profile directory
        run: mkdir -p profile

      # 📊 Github Stats Card
      - name: Generate Stats Card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: stats
          options: username=bgreenwell&show_icons=true&theme=dracula
          path: profile/stats.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      # 💻 Top Languages Card
      - name: Generate Top Languages Card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: top-langs
          options: username=bgreenwell&layout=compact&theme=dracula&hide=html,css
          path: profile/top-langs.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      # 🦀 Rust Projects
      - name: Generate doxx Card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: pin
          options: username=bgreenwell&repo=doxx&theme=dracula
          path: profile/doxx.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate lstr Card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: pin
          options: username=bgreenwell&repo=lstr&theme=dracula
          path: profile/lstr.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate rjot Card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: pin
          options: username=bgreenwell&repo=rjot&theme=dracula
          path: profile/rjot.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate xleak Card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: pin
          options: username=bgreenwell&repo=xleak&theme=dracula
          path: profile/xleak.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      # 🐹 Go Projects
      - name: Generate gitego Card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: pin
          options: username=bgreenwell&repo=gitego&theme=dracula
          path: profile/gitego.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      # 📊 R Packages (CRAN)
      - name: Generate fastshap Card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: pin
          options: username=bgreenwell&repo=fastshap&theme=dracula
          path: profile/fastshap.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate pdp Card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: pin
          options: username=bgreenwell&repo=pdp&theme=dracula
          path: profile/pdp.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate vip Card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: pin
          options: username=koalaverse&repo=vip&theme=dracula
          path: profile/vip.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate sure Card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: pin
          options: username=koalaverse&repo=sure&theme=dracula
          path: profile/sure.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate investr Card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: pin
          options: username=bgreenwell&repo=investr&theme=dracula
          path: profile/investr.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate ebm Card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: pin
          options: username=bgreenwell&repo=ebm&theme=dracula
          path: profile/ebm.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate statlingua Card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: pin
          options: username=bgreenwell&repo=statlingua&theme=dracula
          path: profile/statlingua.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add profile/*.svg
          git commit -m "Update profile badges & stats [skip ci]" || exit 0
          git push
